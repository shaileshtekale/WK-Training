
# program that implements the function of typecasting
number = input("enter any two digit number :")
# here input is in the string format so we need to convert it into integer.

print(float(number[0])+float(number[1]))
# here we are adding the first and second digit of the number
